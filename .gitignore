# 建置產物
/build/
/out/
CMakeUserPresets.json

# IDE
.vs/
*.user

# .vscode 的共用設定進版控（IntelliSense 的 includePath、cmake 完整路徑、建置工作）；
# 只排除個人層級的覆寫。
.vscode/*.local.json
.vscode/launch.local.json

# OS
Thumbs.db
.DS_Store

# 工具套組 lockfile 以外的本機狀態
.claude/settings.local.json
/build-*/
