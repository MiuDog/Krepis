# FND-0002：C ABI 邊界、錯誤模型、記憶體策略與執行緒模型

## 狀態

Proposed

## 日期

提出：2026-08-16

## 背景

這四項是同一個問題的四個面，**必須一起決定**。分開決定會產生互相矛盾的答案：

```
被 Dart FFI 呼叫  →  公開邊界是 C ABI  →  C++ 例外不得跨越  →  錯誤是回傳值
                                       ↓
                          display list 每幀產出  →  arena  →  生命週期契約進邊界
                                       ↓
                          背景執行緒  →  arena 需雙緩衝  →  邊界需執行緒親和性規則
```

`FND-0001` 已固定：公開邊界的形狀由「錯誤會不會靜默」決定，client 不得為權威，
**一條規則只能有一個實作**。本決策在該框架下把四項具體化。

使用者於 2026-08-16 選定**背景執行緒**作為並行模型。

## 決策

### 1. 交付形式：公開邊界是 C ABI

Dart FFI 只認 C ABI，因此：

- 公開標頭只使用 C 相容型別；**C++ 型別、模板、STL 容器一律不得出現在邊界上**。
- 核心物件以 **opaque handle**（不透明指標）暴露，外殼不得知其佈局。
- 進入點帶版本；核心與外殼版本不符時 **fail closed**，不嘗試相容。

C++ 仍是內部實作語言；**C ABI 只是邊界的形狀，不是實作的限制**。

### 2. 錯誤模型

| 層 | 語意 |
|---|---|
| **C ABI 邊界** | 每個函式回傳 typed status code，結果經 out-param 帶出。**邊界必須有 catch-all，任何例外都不得逃逸**——跨 C ABI 拋例外是未定義行為 |
| **內部：可恢復的失敗** | 自訂最小 `Result<T, Error>`。**不為此升到 C++23 取用 `std::expected`**：本專案最終要在 Android NDK 與 Xcode 上建置，降低工具鏈版本需求的價值高於少寫一個型別 |
| **內部：程式錯誤** | assert／terminate。不可恢復，不轉成錯誤碼——把 bug 偽裝成可處理的錯誤會讓它被靜默吞掉 |
| **記憶體不足** | **terminate，不視為可恢復**。arena 策略下配置是 bump，失敗代表系統已無記憶體；為此撐出一條錯誤路徑會污染每一個介面而實務上永遠不被正確處理 |

`Error` 必須含**機器可判斷的 code**。呼叫者不得分析錯誤訊息文字。

### 3. 記憶體策略：arena，雙緩衝

- display list 配置於 **arena（bump allocator）**：一份 display list 一塊 arena，
  用畢整塊丟棄，**不做個別釋放**。一般 heap 在此場景是每幀數千次 malloc／free。
- **雙緩衝**：外殼持有 A 進行繪製時，核心可在 B 建構下一份。
- **生命週期契約（進 C ABI）**：外殼取得的 display list **在其明確釋放該 handle 之前有效**；
  核心不得回收未釋放的緩衝。以「下一幀自動失效」為契約在背景執行緒模型下不成立。
- 一般物件所有權：預設值語意與 RAII、Rule of Zero；`unique_ptr` 表唯一所有權；
  `shared_ptr` **僅用於真正的共同所有權**，不作為方便的預設。

### 4. 執行緒模型：分工，不是整體搬到背景

**「背景執行緒」不表示所有版面都在背景執行。** 若每次按鍵都需一次執行緒交接才會出現字，
那是可見延遲，直接違背本專案的輸入響應目標。

| 路徑 | 執行緒 | 負責 | 預算 |
|---|---|---|---|
| **同步** | 呼叫端（外殼 UI 執行緒） | 影響**當前視窗**的增量重排，以及 display list 產出 | 必須在一幀內完成 |
| **背景** | 核心內部工作執行緒 | 視窗外區域、全文件範圍（總高度／捲軸）、大型重排（載入、視窗縮放） | 無硬預算，但不得阻塞同步路徑 |

**兩條路徑是同一份實作的兩種執行模式，不是兩份實作**（`FND-0001` 第 6 條）。

#### 邊界的執行緒親和性

- **所有 C ABI 進入點只能由外殼的 UI 執行緒呼叫。** 背景執行緒是核心內部細節，永不跨越邊界。
- **核心不得直接回呼外殼。** Dart isolate 是單執行緒，任意原生執行緒無法直接呼叫回 Dart；
  背景工作完成時只能經 Dart native port 送出訊號，由外殼在其 UI 執行緒主動拉取結果。
- 每一個公開函式的文件必須寫明其執行緒契約。

## 這份決策關閉哪一道閘門

**在此之前無法撰寫任何公開標頭**，因為每個函式的形狀（回傳什麼、誰配置記憶體、
哪個執行緒可以呼叫）都未定。

本決策之後，P0 spike 與 P1 的公開介面可以開始撰寫，且新增介面時有機械判準可檢查：
是否為 C 相容型別、是否回傳 status code、是否標明執行緒契約、display list 的釋放責任是否明確。

## 被淘汰的方案

| 方案 | 處置 | 理由 |
|---|---|---|
| 邊界用 C++ ABI | **淘汰** | Dart FFI 不支援。且 C++ ABI 跨編譯器不穩定 |
| 邊界拋例外，外殼接 | **淘汰** | 跨 C ABI 拋例外是未定義行為，不是風格問題 |
| 升到 C++23 用 `std::expected` | **淘汰** | 提高 NDK／Xcode 工具鏈版本需求，換到的只是少寫一個小型別 |
| 每幀重新配置 display list（一般 heap） | **淘汰** | 每幀數千次 malloc／free；且無法表達「外殼還在讀」的生命週期 |
| display list 契約為「下一幀自動失效」 | **淘汰** | 在背景執行緒模型下不成立——外殼可能仍持有前一份 |
| 全部版面搬到背景執行緒 | **淘汰** | 每次按鍵一次執行緒交接＝可見延遲 |
| 全部版面留在 UI 執行緒 | **淘汰** | 長文件的大型重排會卡住輸入 |
| 外殼做樂觀版面以掩蓋延遲 | **淘汰** | 那是版面規則的第二份實作，違反 `FND-0001` 第 6 條，必然靜默分岔 |
| 核心直接回呼外殼 | **淘汰** | Dart isolate 單執行緒，任意原生執行緒無法直接呼叫回 Dart |

## 後果

### 正面

- 公開介面的形狀有機械判準，新增介面時不需重新討論。
- 同步／背景分工讓輸入響應與長文件處理不互相犧牲。
- arena ＋ 明確釋放責任使 display list 的生命週期可被測試驗證。

### 負面

- **C ABI 邊界是持續成本**：每個要暴露的能力都要手寫一層轉接，且不能用 STL 型別。
- **雙路徑的一致性是新的風險**：同步與背景必須對同一份版面狀態操作，
  兩者之間的失效傳播與同步是 P1 的主要難點。
- opaque handle 使外殼難以在除錯器中檢視核心狀態，需要額外的除錯輸出機制。
- 自訂 `Result` 是重複造輪子，且與未來可能的 `std::expected` 遷移有一次性成本。

## 待實測驗證的假設（P0 必須量）

1. **同步路徑真的能在一幀內完成**當前視窗的增量重排 ＋ display list 產出。
   **不成立則本決策的分工失效**，需重新設計。
2. 雙緩衝 arena 的交接成本可忽略。
3. Dart 端零複製讀取 display list 的實際成本與 GC 行為。

## 相關決策

- **本決策依賴**：[`FND-0001`](FND-0001-scope-language-boundary-and-rejections.md)
  （語言邊界判準、client 不得為權威、一條規則只能有一個實作）。
- **依賴本決策**：`BND-*`（FFI 邊界的具體形狀）、`LAY-*`（同步／背景的版面分工、
  display list 的配置與生命週期）、`DOC-*`（核心型別的所有權與執行緒語意）、
  以及所有能力新增公開介面時的形狀檢查。
